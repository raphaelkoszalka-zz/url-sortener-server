ext.database = [
        liquibase : "org.liquibase:liquibase-core",
        postgreSQL: "org.postgresql:postgresql",
        h2        : "com.h2database:h2",
        liquibase : 'org.liquibase:liquibase-core'
]

ext.junit5 = [
        api   : "org.junit.jupiter:junit-jupiter-api",
        engine: "org.junit.jupiter:junit-jupiter-engine"
]

ext.spring = [
        boot : [
                jpa :  "org.springframework.boot:spring-boot-starter-data-jpa",
                jersey : "org.springframework.boot:spring-boot-starter-jersey",
                web : "org.springframework.boot:spring-boot-starter-web"
        ]
]

ex.lombok = [
        plugin : "org.projectlombok:lombok:1.18.4"
]